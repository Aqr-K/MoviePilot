# Example Plugin Database Models

This directory contains database models for the example plugin.
All model classes must follow the naming convention:
- Table names: `plugin_example_{model_name_lowercase}`
- Class names: `PluginExample{ModelName}`

The Base class for this plugin is automatically created by the plugin database system.